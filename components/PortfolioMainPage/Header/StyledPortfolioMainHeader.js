import styled from "styled-components";

export const StyledMainHeader = styled.header`

background-color: var(--primary);
padding:1rem;
`;

export const StyledMainHeaderTitle = styled.h1`
background-color: var(--primary);
`;